<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mediabank.qna.QnaMapper">                                                     
 	<select id="getTotalCount" parameterType="java.util.HashMap" resultType="int">
 		select nvl(count(qna_seq), 0) from qna where #{kind} like '%'||#{search}||'%'
 	</select>
 	<select id="selectList" parameterType="java.util.HashMap" resultType="int">
 		select * from 
		(select rownum R, Q.* from 
		(select * from qna where #{kind} like '%'||#{search}||'%' order by qna_seq desc) Q) 
		where R between #{startRow} and #{lastRow}
 	</select>
</mapper>

